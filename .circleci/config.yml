version: 2
jobs:
  build:
    docker: 
      - image: quay.io/pypa/manylinux1_x86_64
    steps:
      - checkout 
      - run: /opt/python/cp27-cp27mu/python setup.py bdist_wheel
      - run: twine upload dist/*
